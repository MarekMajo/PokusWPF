<UserControl x:Class="MSK_PC_Controller.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid>
        <materialDesign:Card Width="380" Padding="24" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <DockPanel Margin="0,0,0,16">
                    <TextBlock Text="Login" FontSize="22" FontWeight="SemiBold" DockPanel.Dock="Left"/>
                    <Button Content="Theme"
                            Command="{Binding ToggleThemeCommand}"
                            DockPanel.Dock="Right"/>
                </DockPanel>

                <TextBox materialDesign:HintAssist.Hint="Name"
                         Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>

                <!-- pre začiatok: obyč TextBox, neskôr PasswordBox binding/behavior -->
                <TextBox materialDesign:HintAssist.Hint="Password"
                         Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <TextBlock Text="{Binding Error}" Foreground="Tomato" Margin="0,0,0,8"/>

                <Button Content="Login"
                        Command="{Binding LoginCommand}"
                        IsDefault="True"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
